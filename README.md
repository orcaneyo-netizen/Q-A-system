# Q-A-system
A Streamlit‑based Q&amp;A demo that indexes documents with Chroma (dense vectors) and BM25 (sparse). A hybrid retriever merges similarity scores to return the most relevant passages for a user query, displayed in a sleek web UI.
